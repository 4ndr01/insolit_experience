// theme.js

import { createTheme } from '@mui/material/styles';

// Définissez votre thème Material-UI ici
const theme = createTheme({
    palette: {
        primary: {
            main: '#007bff', // Couleur principale
        },
        secondary: {
            main: '#6c757d', // Couleur secondaire
        },
    },
});

export default theme;
